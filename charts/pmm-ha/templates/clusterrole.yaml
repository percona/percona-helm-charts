{{- if .Values.serviceAccount.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "pmm.fullname" . }}
  labels:
    {{- include "pmm.labels" . | nindent 4 }}
rules:
# Standard RBAC for Kubernetes service discovery and monitoring
- apiGroups: [""] # "" indicates the core API group
  resources:
  - namespaces
  - endpoints
  - services
  - nodes
  - nodes/proxy
  - nodes/metrics
  - pods
  - persistentvolumeclaims
  - secrets
  - serviceaccounts
  verbs:
  - get
  - watch
  - list
  - delete
# Access to metrics endpoints for scraping. In nodeExporter.mode=openshift this is also what
# authorizes the scrape: OpenShift's kube-rbac-proxy falls back to a SubjectAccessReview for a
# non-resource "get" on /metrics for any ServiceAccount outside its static allow-list.
- nonResourceURLs:
  - /metrics
  - /metrics/resources
  verbs:
  - get
{{- end }}
