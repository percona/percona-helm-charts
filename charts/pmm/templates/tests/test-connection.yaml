apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pmm.fullname" . }}-test-connection"
  labels:
    {{- include "pmm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pmm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
